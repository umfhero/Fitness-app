export default function Header() {
  return (
    <header
      style={{
        borderBottom: "1px solid var(--border-subtle)",
        padding: "12px 20px",
        background: "var(--bg-surface)",
        fontSize: "14px",
        fontWeight: 500,
      }}
    >
      Trainer Dashboard
    </header>
  )
}